# [PROJECT NAME] — Implementation Guide

This document is the single source of truth for implementing [project name].
It tracks what has been done, what is next, and how to do it.

Read this file at the start of every session before touching any code.
Update the task checklist every time a task is completed.

> This file is generated by `/create-tracker`. Run it on your `.claude/tasks/` directory after all task docs have been written and verified.

---

## Available skills

These Claude Code skills are available in this session. Use them — do not skip them.

**Planning workflow** (use in this order when scoping net-new work):
- `/breakdown` — decompose a feature into executable tasks
- `/write-task` — write a standalone task document for each task
- `/verify-task` — verify all task docs are self-contained (pass the whole `.claude/tasks/` directory)
- `/create-tracker` — generate this IMPLEMENTATION.md from the verified task docs

**Implementation workflow** (use for each task in the checklist):
- `/implement` — implement the scoped functional change defined in the task doc
- `/create-pr` — generate the PR description when implementation is complete

---

## How to use this document

**Starting a new session:**
1. Read this file top to bottom.
2. Find the first unchecked task in the checklist below.
3. Read its task document from `.claude/tasks/`.
4. Use `/implement` to implement it.
5. Use `/create-pr` to generate the PR body. Open the PR.
6. After merge, check the task off in this file.
7. Move to the next unchecked task.

**If a session ends mid-task:**
- Note the current state in the "Session notes" section at the bottom.
- Do not mark the task complete until the PR is merged.
- Next session: read this file, read the session notes, resume where you left off.

---

## Task checklist

Tasks must be completed in the order listed. Check a task off only when its PR is merged into main.

---

### [Task title]
**Task doc:** `.claude/tasks/[filename].md`
**Branch:** `[branch-name]`
**What it does:** [One-line summary of the outcome]
**Depends on:** [Task title, or "Nothing"]

- [ ] [Task title] complete (PR merged)

---

[Repeat block for each task]

---

## Dependency graph

```
[ASCII dependency graph — generated by /create-tracker]

Task A (no dependencies)
  └── Task B (depends on A)
        └── Task C (depends on B)
  └── Task D (depends on A, parallel with B)
```

**Safe serial order:** [Task A] → [Task B] → [Task D] → [Task C]

---

## How to implement a task

Follow these steps exactly. Do not skip any step.

### 1. Read before writing

Open the task document. Read every section: Outcome, Why, Context, Scope, Out of Scope, Acceptance Criteria.

Before writing any code, output:
```
TASK: [task title]
BRANCH: [branch name]
I WILL CREATE/MODIFY:
  - [file] — [what it does]
OUT OF SCOPE (will not touch):
  - [file or area]
ACCEPTANCE CRITERIA:
  1. [criterion]
```

If anything is unclear, stop and ask. Do not guess.

### 2. Create the branch

```bash
git checkout main
git pull origin main
git checkout -b [branch-name]
```

### 3. Implement

Use `/implement` with the task description. Only modify files in the task doc's Scope. Run typecheck and formatter before committing.

### 4. Verify

Go through every Acceptance Criterion. Do not claim done until all pass.

### 5. Commit and PR

```
feat([branch-name]): [short description]
```

Use `/create-pr` to generate the PR body. Open the PR.

### 6. After merge

Check the task off above. Pull main. Move to the next task.

---

## File locations reference

| What | Where |
|---|---|
| Task documents | `.claude/tasks/` |
| This file | `IMPLEMENTATION.md` |
| Project CLAUDE.md | `CLAUDE.md` |
| [Project-specific locations] | [paths] |

---

## Session notes

```
Last session: [date]
Current task: [task name]
Status: [what was done, what is not yet done]
Branch: [current branch name]
Next action: [exactly what to do when resuming]
```
